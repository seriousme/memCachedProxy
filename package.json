{
  "name": "memcachedproxy",
  "version": "0.0.1",
  "description": "a memcached server with a DynamoDB backend",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "node index.js",
    "test": "tap test/*.js",
    "testCoverage": "tap test/*.js --cov",
    "testCoverageHTML": "tap test/*.js  --coverage-report=html"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.480.0",
    "bl": "^3.0.0",
    "lru-cache": "^5.1.1",
    "memcached": "^2.2.2"
  },
  "devDependencies": {
    "tap": "^14.2.5"
  }
}
